- name: Setup and Run Synopsys Bridge
      env:
        COVERITY_URL: ${{ secrets.COVERITY_URL }}
        COVERITY_USER: ${{ secrets.COVERITY_USER }}
        COVERITY_PASSPHRASE: ${{ secrets.COVERITY_PASSPHRASE }}
      run: |
        # Descarga la versión estable usando la API de Synopsys
        curl -fLO https://sig-repo.synopsys.com/artifactory/bds-integrations-release/com/synopsys/integration/synopsys-bridge/latest/synopsys-bridge-latest-linux64.zip
        unzip -o synopsys-bridge-latest-linux64.zip
        chmod +x bridge
        
        # Ejecuta el escaneo
        ./bridge --stage connect \
          --coverity.connect.url=$COVERITY_URL \
          --coverity.connect.user=$COVERITY_USER \
          --coverity.connect.password=$COVERITY_PASSPHRASE \
          --coverity.connect.project.name="${{ github.event.repository.name }}" \
          --coverity.connect.stream.name="main"